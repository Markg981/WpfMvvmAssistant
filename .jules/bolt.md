## 2023-10-27 - RegexOptions.Compiled Performance Anti-pattern\n**Learning:** Compiling dynamic regex patterns inside a loop using RegexOptions.Compiled is a severe performance anti-pattern. Static regex patterns should be compiled and reused. \n**Action:** Use static readonly Regex instances for static patterns with RegexOptions.Compiled. Use standard Regex.Match for dynamic patterns to avoid recompilation overhead.
