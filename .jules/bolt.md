## 2024-05-24 - The execution environment is missing a dependency (Microsoft.Data.SqlClient) in Core.Domain.csproj that breaks compilation.
**Learning:** The execution environment has a broken baseline setup due to missing Microsoft.Data.SqlClient in the Domain layer. Adding it would be a severe Clean Architecture violation as instructed in the prompt memory ("The baseline repository has a missing dependency (`Microsoft.Data.SqlClient`) in `Core.Domain.csproj` that breaks compilation. However, adding it is a severe Clean/Onion architecture violation, as the Domain layer should not depend on Data Access packages. Do not alter `.csproj` dependencies to fix this test execution environment issue.") Therefore, `dotnet test` will predictably fail.
**Action:** Proceed with submission despite test failure, recognizing it is an execution environment issue and should not block the PR.
